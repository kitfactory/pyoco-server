# delta-request

## Delta ID
- DR-20260401-version-070

## Delta Type
- DOCS-SYNC

## 目的
- `pyoco-server` の現在バージョン表記を `0.7.0` に更新する。

## 変更対象（In Scope）
- `pyproject.toml` の package version。
- アプリケーション metadata の version 表記。
- README / OVERVIEW の現在バージョン表記。
- `docs/plan.md` と delta 記録。

## 非対象（Out of Scope）
- `pyoco>=0.6.2` 依存バージョンの変更。
- API / 実装仕様の変更。
- テスト修正や機能追加。

## 差分仕様
- DS-01:
  - Given: 現在 `pyoco-server` のバージョン表記が `0.5.1` である。
  - When: バージョン同期を行う。
  - Then: package / app / docs の対象箇所が `0.7.0` に更新される。

## 受入条件（Acceptance Criteria）
- AC-01: `pyproject.toml` の `version` が `0.7.0` である。
- AC-02: `src/pyoco_server/http_api.py` の FastAPI version が `0.7.0` である。
- AC-03: README / OVERVIEW の現在バージョン表記が `0.7.0` である。

## 制約
- `pyoco 0.6.2` という依存・前提の記述はそのまま維持する。

## Review Gate
- required: No
- reason: version metadata と docs 表記の同期のみのため。

## 未確定事項
- なし

## Step 2: delta-apply
- status: APPLIED
- changed files:
  - `pyproject.toml`
  - `src/pyoco_server/http_api.py`
  - `README.md`
  - `docs/OVERVIEW.md`
  - `docs/plan.md`
- applied AC:
  - AC-01:
    - package version を `0.7.0` に更新した。
  - AC-02:
    - FastAPI metadata version を `0.7.0` に更新した。
  - AC-03:
    - README / OVERVIEW の現在バージョン表記を `0.7.0` に更新した。
- non-scope check:
  - Out of Scope への変更なし: Yes

## Step 3: delta-verify
- verify profile:
  - static check:
    - `python -m compileall src`
  - targeted grep:
    - `rg -n "0\\.5\\.1|0\\.7\\.0" pyproject.toml src docs README.md`
- AC result table:
  - AC-01: PASS
  - AC-02: PASS
  - AC-03: PASS
- scope deviation:
  - Out of Scope 変更の有無: No
- overall: PASS

## Step 4: delta-archive
- verify result: PASS
- review gate: NOT REQUIRED
- archive status: archived
- unresolved items:
  - なし
- follow-up delta seeds:
  - なし

## Canonical Sync
- synced docs:
  - concept: No change
  - spec: No change
  - architecture: No change
  - plan: `docs/plan.md`
